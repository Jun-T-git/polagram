name: Dev View
layers:
  - action: focus
    selector:
      kind: participant
      name:
        pattern: "API|Cache|Database"
  - action: resolve
    selector:
      kind: fragment
      condition: "Cache Miss"
